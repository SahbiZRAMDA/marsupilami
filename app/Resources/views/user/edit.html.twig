{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-xl-8 col-lg-12 tm-md-12 tm-sm-12 tm-col">
        <div class="bg-white tm-block h-100">
            <h1>User edit</h1>

            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input class="btn btn-small btn-primary left" type="submit" value="Edit"/>
            {{ form_end(edit_form) }}
            <a class="btn btn-small btn-primary right" href="{{ path('homepage') }}">Back to the list</a>
        </div>
    </div>
{% endblock %}
